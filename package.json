{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3"
  },
  "scripts": {
    "local-deploy": "npx hardhat run --network localhost scripts/deploy-token.js",
    "scroll-deploy":"npx hardhat run --network scrollAlpha scripts/deploy-token.js",
    "test": "npx hardhat test"
  }
}
